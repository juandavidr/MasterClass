<% if current_user.usertype.eql?("Coordinador") || current_user.usertype.eql?("Admin") %>
  <div id="main1">
    <div id="main2">
      <div id="left">
        <div class="column-in">
          <h2>
            <p id="lccont" class="btn">
              <%= link_to "Agregar materia", subjects_new_path %>
            </p>
          </h2>
        </div>
      </div>
    </div>
  </div>
<% end %>

<% unless @subject_list.nil? %>
  <% if @subject_list.size>0 %>
    <table  border="1">
      <th>Código</th>
      <th>Nombre</th>
      <th>Quota</th>
      <th>Estado</th>
          <% if current_user.usertype.eql?("Coordinador") || current_user.usertype.eql?("Admin") %>
      <th colspan="3">Acciones</th>
      <% end %>
      <%= render @subject_list %>
    </table>
    <%= will_paginate @subject_list  %>


  <% else %> 
    <p>No existen materias disponibles</p>
  <% end %>  
<% end %>
