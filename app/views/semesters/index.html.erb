<% if current_user.usertype.eql?("Coordinador") || current_user.usertype.eql?("Admin") %>
  <div id="main1">
    <div id="main2">
      <div id="left">
        <div class="column-in">
          <h2>
            <p id="lccont" class="btn">
              <%= link_to "Agregar semestre", semesters_new_path %>
            </p>
          </h2>
        </div>
      </div>
    </div>
  </div>
<% end %>

<% unless @semester_list.nil? %>
  <% if @semester_list.size>0 %>
    <table  border="1">
      <th>Nombre</th>
      <th>Tipo</th>
      <th>Fecha de inicio</th>
      <th>Fecha de finalización</th>
      <th>Estado</th>
      <% if current_user.usertype.eql?("Coordinador") || current_user.usertype.eql?("Admin") %>
        <th colspan="3">Acciones</th>
      <% end %>

      <%= render @semester_list %>
    </table>
    <%= will_paginate @semester_list  %>
  <% else %> 
    <p>No existen programas disponibles</p>
  <% end %>  
<% end %>